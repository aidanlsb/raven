name: raven-query
description: Raven query language, saved queries, search, and link traversal.
default_prompt: Use Raven CLI (`rvn`) query commands with --json. Build precise object, trait, section, or link queries and preview before any bulk apply.
