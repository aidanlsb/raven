name: raven-schema
description: Raven schema modeling and migration workflows.
default_prompt: Use Raven schema commands with --json, or the equivalent Raven MCP tools in MCP sessions, and validate before and after schema changes.
