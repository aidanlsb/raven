name: raven-vault-admin
description: Raven vault setup, active/default vault routing, and global plus vault-local config workflows.
default_prompt: Manage Raven vaults, global config, and vault-local config with explicit vault selection using Raven CLI (`rvn`) commands with --json.
