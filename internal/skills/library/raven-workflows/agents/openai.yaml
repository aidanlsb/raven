name: raven-workflows
description: Raven workflow authoring, execution, continuation, and run-maintenance patterns.
default_prompt: Build and run Raven workflows with validation after each mutation and resume paused runs via workflow continue.
