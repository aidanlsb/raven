id: raven-templates
title: Raven Templates
version: 1
summary: Template file lifecycle and schema/template binding workflows.
entry: body.md
references:
  - references/template-lifecycle.md
coverage:
  commands:
    - template
    - template list
    - template write
    - template delete
    - schema template list
    - schema template get
    - schema template set
    - schema template remove
    - schema template bind
    - schema template unbind
    - schema template default
execution:
  preferred_transport: cli
  cli_binary: rvn
  cli_args_suffix:
    - --json
  mcp_fallback: true
