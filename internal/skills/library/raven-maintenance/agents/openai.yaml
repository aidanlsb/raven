name: raven-maintenance
description: Vault health checks, reindexing, data import, and documentation access.
default_prompt: Use Raven maintenance commands with --json for vault health checks, reindexing, and data import. Preview before applying fixes or imports.
