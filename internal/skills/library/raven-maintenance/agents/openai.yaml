name: raven-maintenance
description: Vault health checks, reindexing, and data import.
default_prompt: Use Raven CLI (`rvn`) maintenance commands with --json for vault health checks, reindexing, and data import. Preview before applying fixes or imports.
