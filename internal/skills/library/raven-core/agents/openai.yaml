name: raven-core
description: Core Raven workflows for creating, querying, and editing content.
default_prompt: Use Raven CLI commands with --json for safe deterministic vault operations, or the equivalent Raven MCP tools when already operating through MCP.
