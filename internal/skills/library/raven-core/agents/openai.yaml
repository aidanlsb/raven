name: raven-core
description: Core Raven workflows for creating, reading, editing, and organizing content.
default_prompt: Use Raven CLI (`rvn`) commands with --json for safe deterministic vault operations.
