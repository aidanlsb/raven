# Specification (deprecated)

This repository used to maintain a single “mega spec” here. It has been split into:

- `docs/guide/` — task-oriented docs
- `docs/reference/` — canonical specs (syntax/config/contracts)
- `docs/design/` — internal design notes

Start at `docs/README.md`.

